kind = "web"
previewPath = "/"
title = "Syahdan Triantoro — Portfolio"
version = "1.0.0"
id = "artifacts/portfolio"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21113

[services.development]
run = "pnpm --filter portfolio run dev"

[services.production]
build = [ "pnpm", "--filter", "portfolio", "run", "build" ]
serve = "static"
publicDir = "artifacts/portfolio/dist"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21113"
BASE_PATH = "/"
